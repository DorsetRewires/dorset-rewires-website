# ops/ stays on Pete's PC only - never committed, never deployed
ops/

# Secrets - API keys live here, never committed (repo may be public)
.env
*.env

# Cloudflare / build artifacts
.cloudflare/
.wrangler/

# Python
__pycache__/
*.pyc
*.pyo

# Node (used only for Decap CMS local dev server)
node_modules/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.swp
*.swo

# Logs
*.log

# Local Claude launchers (machine-specific, not for deploy)
open new session.bat
existing session.bat

# Claude working folder (internal, not for the public website repo)
Claude/

# Claude project instructions (internal notes, not for the public website repo)
CLAUDE.md

# Internal scratch / prompts (not for the public website repo)
non context website checks.txt

# Client / architect job files (e.g. tender drawings, specs) - NEVER commit; repo may be public
projects/

# EICR phone app - its OWN private git repo (DorsetRewires/eicr-app), never part of the public website repo
eicr-app/
